<view>
  <view class="xs">
    <view class="xsItem" wx:for="{{xsItem}}" wx:for-item="item" wx:key="index">
      <image src="{{item.imgUrl}}"></image>
      <view bindtap="selectBg" data-url="{{index}}">{{item.name}}</view>
    </view>

  </view>
</view>